#!/usr/bin/env zsh

# Profile Custom Script

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# remove .sudo if exists
if [ -f "$HOME/.sudo" ]; then
  rm -Rf "$HOME/.sudo"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

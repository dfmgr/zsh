#!/usr/bin/env zsh
# Profile Custom Script
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# remove .sudo if exists
if [[ -f "$HOME/.sudo" ]]; then
  rm -f "$HOME/.sudo"
fi
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
